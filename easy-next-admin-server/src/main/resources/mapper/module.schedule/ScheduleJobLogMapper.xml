<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laker.admin.module.schedule.mapper.ScheduleJobLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.laker.admin.module.schedule.entity.ScheduleJobLog">
        <id column="job_log_id" property="jobLogId" />
        <result column="job_code" property="jobCode" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="status" property="status" />
        <result column="cost" property="cost" />
        <result column="thread_name" property="threadName" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        job_log_id, job_code, start_time, end_time, status, cost, thread_name
    </sql>

</mapper>
